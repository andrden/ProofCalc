$a (x + y) * z = x * z + y * z

$a y * x - z * x = ( y - z ) * x

$a x * y - x * z = x * (y - z)

$a (x - z) * y = (z - x) * (- y)

$a ( ch x ) ^ 2 - ( sh x ) ^ 2 = 1

$? x * ( ch ψ ) ^ 2 - x * ( sh ψ ) ^ 2
$= x

$? x * ( ch ψ ) ^ 2 - y * ( ch ψ ) ^ 2 + y * ( sh ψ ) ^ 2 - x * ( sh ψ ) ^ 2
$= x - y

$? (x - y) * ( ch ψ ) ^ 2 + (y - x) * ( sh ψ ) ^ 2
$= x - y
